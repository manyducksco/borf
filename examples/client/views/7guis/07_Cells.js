import { makeView } from "@woofjs/client";

export default makeView((ctx) => {
  ctx.name = "7guis:Cells";

  return (
    <div class="example">
      <header>
        <h3>Cells</h3>
      </header>

      <div></div>
    </div>
  );
});
