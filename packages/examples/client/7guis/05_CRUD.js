import { makeComponent } from "@woofjs/client";

export default makeComponent(($, self) => {
  return (
    <div class="example">
      <header>
        <h3>CRUD</h3>
      </header>

      <div></div>
    </div>
  );
});
