// const record = await app.data.find("record", 5);
// const record = app.data.create("record"); // create empty default record

// record.field = 5;

// await record.save();
// await record.revert(); // undo all unsaved changes

// record.listen(fn); // listen for changes to any field
// record.listen("field", fn); // or only one field
// record.bind("field"); // create binding to a field
