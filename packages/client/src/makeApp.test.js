test.skip("nested routes are parsed correctly", () => {});
test.skip("lifecycle", () => {
  // beforeConnect and afterConnect are run
  // app waits for beforeConnect promise to resolve before connecting
});
