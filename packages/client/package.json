{
  "name": "@manyducksco/woof",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.js",
  "main": "dist/woof.js",
  "types": "woof.d.ts",
  "module": "dist/woof.js",
  "scripts": {
    "demo": "http-server demo",
    "test": "jest",
    "test:watch": "npm run test -- --watch",
    "test:coverage": "npm run test -- --coverage",
    "build:test": "esbuild src/testing/index.js --platform=browser --target=es2018 --format=esm --bundle --sourcemap --outfile=dist/testing.js",
    "build": "esbuild woof=src/index.js --platform=browser --target=es2018 --format=esm --bundle --sourcemap --outfile=dist/woof.js",
    "build:node": "esbuild woof=src/index.js --platform=node --target=es2015 --format=cjs --bundle --sourcemap --outfile=dist/woof.node.js",
    "build:watch": "npm run build -- --watch",
    "build:prod": "npm run build:testing -- --minify && npm run build -- --minify"
  },
  "keywords": [],
  "author": "Tony McCoy <tony@thatsalotofducks.com>",
  "license": "ISC",
  "devDependencies": {
    "@testing-library/dom": "^8.11.1",
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.7",
    "cross-fetch": "^3.1.4",
    "es-jest": "^1.5.0",
    "esbuild": "^0.13.13",
    "history": "^5.1.0",
    "http-server": "^14.0.0",
    "jest": "^27.3.1",
    "msw": "^0.35.0",
    "path-to-regexp": "^6.2.0",
    "query-string": "^7.0.1"
  }
}
