import { Widget } from "../Widget.js";

/**
 * A widget that allows the user to input a number value for an attribute.
 */
export class NumberWidget extends Widget {
  constructor(initialValue: number) {
    super();
  }
}
