import { Widget } from "../Widget.js";

/**
 * A widget that allows the user to input a freeform string value for an attribute.
 */
export class StringWidget extends Widget {
  constructor(initialValue: string) {
    super();
  }
}
