import { Resource } from "@manyducksco/woof/server";

export class {{name}} extends Resource {
  _map(route) {
    route.get("", this.search); // get index
    route.post("", this.create);
    route.get(":id", this.get);
    route.put(":id", this.update);
    route.delete(":id", this.destroy);
  }

  async getMessage(ctx) {
    return {
      message: "Hello from {{name}}"
    };
  }

  async search(ctx) {
    let { limit, page, ...query } = ctx.query;

    
  }
}
