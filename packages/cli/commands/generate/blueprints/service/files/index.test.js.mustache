import { makeSuite, wrapService, MockHTTP } from "@woofjs/app/testing";
import {{name}} from "./index.js";

export default makeSuite((test) => {
  const http = new MockHTTP();

  http.get("/mock/route", (req, res, ctx) => {
    res(ctx.json({
      message: "replace me"
    }));
  });

  const createService = wrapService({{name}})
    .service("@http", http.service);

  test("is implemented", (t) => {
    const service = createService();
  
    t.equal(true, false);
  });
})

