import { makeSuite, wrapComponent } from "@woofjs/app/testing";
import {{name}} from "./index.jsx";

export default makeSuite((test, view) => {
  const initComponent = wrapComponent({{name}});

  view("default", ($) => {
    return initComponent();
  });

  test("is implemented", (t) => {
    t.assert(false, "is implemented");
  });
});
